# Bugs (открытые)

## Критические
- нет

## Известные
- `sort-header` dataset.sort не работает с CH24h/NATR в V2 (исправить data-sort атрибуты)
- tmux send-keys C-c убивает сессию (нужен более надёжный способ перезапуска)
- При первом запуске /api/tickers может вернуть пустой NATR (нужно время на сбор)

## Статус
- V2 на порту 5000 (через tmux)
- V1 доступен по `git checkout` (старый index.html в корне)
- Vite dev server не используется (только build)
